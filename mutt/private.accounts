set spoolfile   = "+private/INBOX"
set postponed   = "+private/drafts"
set record    	= "+private/sent"

set from      	= "leon@leon.nu"
set pgp_autosign = no
set maildir_trash = yes
set sort_aux = reverse-last-date-received

macro index,pager <F5> "<pipe-message>~/src/b4/b4.sh mbox -f -o ~/mail/$spoolfile/<return>
